start
docker-compose up --build

to empty db before new migration
docker exec backend_db_1 psql -U postgres -d postgres -c "TRUNCATE TABLE users, sessions RESTART IDENTITY;"

incase it doesnt work
docker exec backend_db_1 psql -U postgres -d postgres -c "DROP SCHEMA public CASCADE; CREATE SCHEMA public;"

generate new db
docker-compose exec web alembic revision --autogenerate
docker-compose exec web alembic upgrade head
